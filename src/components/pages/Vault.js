import React from "react";
// import MovieList from "./MovieList";
import Vault from "../pages/Vault";

const vault = (props) => (
	<div>
		<h2>Movie Night Potential Picks</h2>
		<Vault />
	</div>
);

export default vault;
